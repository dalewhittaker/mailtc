----------
NEXT RELEASE:
1) Test all new plugin stuff lots with valgrind
        i) test tabs work correctly
        ii) test every plugin routine (e.g clicked, get_config etc) work, for both minimal and non-minimal
        iii) test filter error reporting is ok
----------

WISH LIST TASKS:
1) Threading, allowing reading (click) when checking new mail (big job).
