----------
NEXT RELEASE:
1) Rework plugin API so that the filters can be moved there
    a) make sure everything works with MTC_NOTMINIMAL not defined (i.e minimal build)
    b) get plugins working nicely together, e.g the dialogs
    c) test lots with valgrind
----------

WISH LIST TASKS:
1) Threading, allowing reading (click) when checking new mail (big job).
