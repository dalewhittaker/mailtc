----------
NEXT RELEASE:
1) Rework plugin API so that the filters can be moved there
    a) protocol_combo_changed needs big testing with valgrind, only 1 call should be made, check this.
    
    a) use dummy as reference and move filters over
        i) get_config
        ii) put_config
        iii) write_config
        iv) read_config
        v) get above working for one plugin, then working for all
    b) make sure everything works with MTC_NOTMINIMAL not defined (i.e minimal build)
    c) test lots with valgrind
----------

WISH LIST TASKS:
1) Threading, allowing reading (click) when checking new mail (big job).
