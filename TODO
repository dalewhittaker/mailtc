NEXT RELEASE:
1)  Valgrind. Test filters with plugins. Test config dialog with plugins.
	Test all network stuff (i.e no connection/wrong password etc) for each plugin.
	Test docklet click stuff.
	Test all GSList stuff.
	Test app launching.
	Test everything basically.
2) Add the French translation (from Val) package the bugger up, do the usual stuff

RELEASE AFTER
1) Improve plugin architecture so that it is possible to have plugin specific options in config dialog. Possibly improve filter disabling too.

------------------------------
***TO ADD NEW LANG***
use msginit -l <lang> in po dir to create po file
translate the po file
add the lang to ALL_LINGUAS in configure.ac
aclocal;autoconf;automake
./configure;make etc
