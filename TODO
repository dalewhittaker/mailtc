NEXT RELEASE
1) need to add 'po' and 'include' dir to svn
2) fix problem with app launching (/usr/bin/sylpheed has problems, but sylpheed does not, because of the dir)
3) package the bugger up, do the usual stuff

***TO ADD NEW LANG***
use msginit -l <lang> in po dir to create po file
translate the po file
add the lang to ALL_LINGUAS in configure.ac
aclocal;autoconf;automake
./configure;make etc

ONE AFTER
1) Make modular (with network plugins)
